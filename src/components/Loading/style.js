import styled from 'styled-components'

export const Container = styled.div`
    height: calc(60vh);

    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;

    font-size: 1.6rem;

    img {
        width: 60px;
    }
`